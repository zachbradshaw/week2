var hangmanWords;
var guess;

var validWords = hangmanWords.filter(array) {
  return array.length >= 3;
}

console.log(validWords);

function setGuess (guessValueString) {
  guessValue = Number(guessValueString);
}

function checkGuess ()
